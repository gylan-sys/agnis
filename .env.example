# Production Environment Variables
NODE_ENV=production
PORT=3000

# Backend/Database Settings
DATA_DIR=/app/data

# Frontend Settings (Vite)
# Prefix with VITE_ if you want to access in React code
VITE_API_URL=/api
